/// Dimensões e espaçamentos extraídos do design do Figma
class AppDimensions {
  AppDimensions._();

  /// Tamanho do logo (412px conforme especificado no Figma)
  static const double logoSize = 412.0;
  
  /// Espaçamento padrão
  static const double defaultPadding = 16.0;
  
  /// Espaçamento pequeno
  static const double smallPadding = 8.0;
  
  /// Espaçamento grande
  static const double largePadding = 32.0;
}
