/// Use case responsável por navegar para a tela de cadastro
class NavigateToSignUpUseCase {
  /// Executa a navegação para a tela de cadastro
  Future<void> call() async {
    // Aqui você pode adicionar lógica de navegação ou validações
    // Por exemplo: verificar conectividade, logs de analytics, etc.
    
    // Por enquanto é apenas um placeholder para navegação
    await Future.delayed(const Duration(milliseconds: 100));
  }
}

/// Use case responsável por navegar para a tela de login
class NavigateToLoginUseCase {
  /// Executa a navegação para a tela de login
  Future<void> call() async {
    // Aqui você pode adicionar lógica de navegação ou validações
    // Por exemplo: verificar se o usuário já tem conta, logs de analytics, etc.
    
    // Por enquanto é apenas um placeholder para navegação
    await Future.delayed(const Duration(milliseconds: 100));
  }
}
